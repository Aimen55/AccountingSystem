<div class="jumbotron jumbotron-fluid">
<div class="container text-center">
    <h1>Account Types</h1>
</div>
</div>








<div class="container">
  <nav class="navbar navbar-default">
  <div class="container-fluid">
    <p class="navbar-text pull-right">
     <%= link_to "new Account Type", new_account_type_path %>
    </p>
  </div>
  </nav>
  <div class="col-md-12">
    <table id="accounts_table" class="table table-striped table-bordered" style="width:100%">
      <thead>
        <tr>
          <th>Name</th>
          <th ></th>
          <th></th>
          <th></th>
        </tr>
      </thead>
      <tbody>
        <% @account_types.each do |account_type|%>
          <tr>
            <td><%= account_type.name %></td>
            <!-- <td>< #%= image_tag url_for(account.picture) rescue '' %></td> -->
            <!-- <td><%# if account.picture.attached? %> -->
                  <%#= image_tag url_for(account.picture)  %>
                <%#else %>
                  <!-- <div class="text-align-center img-place-holder"> -->
                       <!-- self.picture.attach(io: File.open(Rails.root.join("app", "assets", "images", "default.jpeg")), filename: 'default.jpg' , content_type: "image/jpg") -->
                  <!-- </div> -->
                <!-- <%# end %></td> -->
            <td><%= link_to 'show',account_type_path(account_type) %></td>
            <td><%= link_to 'Edit', edit_account_type_path(account_type) %></td>
            <td><%= link_to 'Destroy', account_type_path(account_type),
                    method: :delete,
                    data: { confirm: 'Are you sure?' } %></td>
          </tr>
           <% end %>   
      </tbody>
    </table>
  </div>

<br>

</div>

<!-- <script type="text/javascript"> -->
  <!-- $(document).ready(function() { -->
      <!-- $('#accounts_table').DataTable(); -->
  <!-- } ); -->
<!-- </script> -->